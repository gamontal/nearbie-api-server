# APACHE BENCH LOAD TESTING

# Some common ab flags definitions:

# -p means to POST it
# -H adds an Auth header (could be Basic or Token)
# -T sets the Content-Type
# -c is concurrent clients
# -n is the number of requests to run in the test
